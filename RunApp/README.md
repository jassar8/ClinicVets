# RunApp — official ClinicVets application

All builds and the portable EXE come from this folder only.

- **Run:** `Run.bat` or `dotnet run --project ClinicVets.csproj`
- **Publish EXE:** `Publish-Avalonia-WinX64.ps1` → `Publish\ClinicVets.exe`

See repo root **README.md** for full documentation.
