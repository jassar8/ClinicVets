# תשובת מטלה — ניהול בעלי חיים

**פרויקט:** ClinicVets — מערכת ניהול מרפאה וטרינרית  
**תחום:** הוספת חיה חדשה, קטalog סוגי חיות, ניהול וסינון רשימת בעלי חיים  
**קבצים רלוונטיים בפרויקט:**  
`AnimalsView`, `ValidationService`, `AppData`, `Animal`

---

## 1. CFG — גרף זרימת בקרה (Control Flow Graph)

נבחרו **שתי פונקציות אמיתיות** מהקוד. כל אחת כוללת תנאים (`if`) או לולאה / `switch`.

---

### 1.1 פונקציה ראשונה: `IsValidChipNumber`

**מטרה:** לבדוק שמספר השבב תקין — 7 ספרות, מתחיל ב-`376`.  
**מיקום:** [`Source/Services/ValidationService.cs`](Source/Services/ValidationService.cs) — שורות 213–219  
**Class:** `ValidationService`  
**Method:** `public static bool IsValidChipNumber(string chipNumber)`

#### קוד הפונקציה (מתוך הפרויקט)

```csharp
public static bool IsValidChipNumber(string chipNumber)
{
    return !string.IsNullOrWhiteSpace(chipNumber) &&
           chipNumber.Length == 7 &&
           chipNumber.StartsWith("376", StringComparison.Ordinal) &&
           chipNumber.All(char.IsDigit);
}
```

#### הסבר CFG — `IsValidChipNumber`

**צמתים (Nodes):**

| מזהה | תיאור |
|------|--------|
| C1 | התחלה — קלט: `chipNumber` |
| C2 | האם `chipNumber` לא ריק? |
| C3 | האם אורך == 7? |
| C4 | האם מתחיל ב-`376`? |
| C5 | האם כל התווים ספרות? |
| C6 | החזר `true` |
| C7 | החזר `false` |
| C8 | סיום |

**קשתות (Edges):**

```
C1 → C2
C2 --[לא]--> C7 → C8
C2 --[כן]--> C3
C3 --[לא]--> C7
C3 --[כן]--> C4
C4 --[לא]--> C7
C4 --[כן]--> C5
C5 --[לא]--> C7
C5 --[כן]--> C6 → C8
```

**תרשים מילולי:**

```
     [C1 התחלה]
          |
   [C2 לא ריק?]---לא--->[C7 false]--->[C8]
          |כן
   [C3 אורך 7?]---לא--->[C7]
          |כן
   [C4 מתחיל 376?]---לא--->[C7]
          |כן
   [C5 כולם ספרות?]---לא--->[C7]
          |כן
   [C6 true]--->[C8 סיום]
```

---

### 1.2 פונקציה שנייה: `MatchesSearchAndFilter`

**מטרה:** לסנן חיה לפי טקסט חיפוש (שם / שבב) ולפי סוג חיה או סטטוס חיסון.  
**מיקום:** [`Source/Frontend/Views/AnimalsView.axaml.cs`](Source/Frontend/Views/AnimalsView.axaml.cs) — שורות 688–708  
**Class:** `AnimalsView`  
**Method:** `private bool MatchesSearchAndFilter(Animal animal)`

#### קוד הפונקציה (מתוך הפרויקט)

```csharp
private bool MatchesSearchAndFilter(Animal animal)
{
    string searchText = AnimalSearchInput?.Text?.Trim() ?? "";
    string filter = GetSelectedAnimalFilter();

    bool matchesSearch = string.IsNullOrWhiteSpace(searchText) ||
                         animal.Name.Contains(searchText, StringComparison.OrdinalIgnoreCase) ||
                         animal.ChipNumber.Contains(searchText, StringComparison.OrdinalIgnoreCase);

    bool matchesFilter = filter switch
    {
        "כלב" => animal.Species == "כלב" || animal.Species == "Dog",
        "חתול" => animal.Species == "חתול" || animal.Species == "Cat",
        "זוחל" => animal.Species == "זוחל" || animal.Species == "Reptile",
        "ציפור" => animal.Species == "ציפור" || animal.Species == "Bird",
        "צריך חיסון" => ValidationService.IsVaccinationDue(animal.LastVaccinationDate),
        _ => true
    };

    return matchesSearch && matchesFilter;
}
```

#### הסבר CFG — `MatchesSearchAndFilter`

**צמתים (Nodes):**

| מזהה | תיאור |
|------|--------|
| F1 | התחלה — קלט: `animal` |
| F2 | קריאת `searchText` ו-`filter` |
| F3 | האם חיפוש ריק **או** שם מכיל **או** שבב מכיל? |
| F4 | בחירת ענף `switch` לפי `filter` |
| F5 | האם `matchesSearch && matchesFilter`? |
| F6 | החזר `true` |
| F7 | החזר `false` |
| F8 | סיום |

**קשתות (Edges):**

```
F1 → F2 → F3
F3 --[לא תואם חיפוש]--> F7 → F8
F3 --[תואם חיפוש]--> F4
F4 --[לא תואם סינון]--> F7
F4 --[תואם סינון]--> F6 → F8
```

---

## 2. שלוש User Stories (סיפורי משתמש)

### US-01 — הוספת חיה חדשה

**As a** מזכיר/ה או וטרינר/ית במרפאה,  
**I want** להוסיף חיה חדשה ולקשר אותה לבעלים קיים,  
**So that** אוכל לנהל את רישום החיות במרפאה בצורה מסודרת.

**קריטריוני קבלה:**
- בחירת בעלים מרשימת לקוחות.
- מילוי שם, סוג (כלב/חתול/זוחל/ציפור), שבב, משקל, תאריכים.
- ולידציה לפי `ValidationService` לפני שמירה.
- שמירה ל-SQLite דרך `AppData.SaveAnimalsToDatabase()`.

---

### US-02 — סינון קטalog חיות לפי סוג

**As a** עובד/ת במרפאה,  
**I want** לסנן את רשימת החיות לפי סוג (כלב, חתול, זוחל, ציפור) או לפי "צריך חיסון",  
**So that** אוכל למצוא במהירות חיות רלוונטיות לטיפול.

**קריטריוני קבלה:**
- ComboBox סינון במסך בעלי חיים.
- `MatchesSearchAndFilter` מציג רק כרטיסים מתאימים.
- הודעה "לא נמצאו בעלי חיים שמתאימים לחיפוש" כשאין תוצאות.

---

### US-03 — חיפוש חיה לפי שם או שבב

**As a** וטרינר/ית,  
**I want** לחפש חיה לפי שם או מספר שבב,  
**So that** אוכל לפתוח את כרטיס החיה ולעדכן פרטים.

**קריטריוני קבלה:**
- שדה חיפוש מעדכן את הרשימה בזמן אמת (`AnimalSearch_Changed`).
- לחיצה על כרטיס טוענת את הפרטים לטופס.

---

## 3. ארבעה מקרי בדיקה (Test Cases)

מקרי הבדיקה מגיעים מ-**US-01**, **US-02** ו-**US-03**.

---

### TC-01

| שדה | ערך |
|-----|-----|
| **Test Case ID** | TC-01 |
| **Test Case Name** | הוספת חיה חדשה — נתונים תקינים |
| **Type** | Functional — Positive |
| **Test Data** | שם: `באדי`, סוג: כלב, שבב: `3761234`, משקל: `12`, בעלים: לקוח קיים, תאריך לידה ≥ 2000, חיסון ≤ היום |
| **Expected Result** | הודעה "החיה נוספה בהצלחה"; הכרטיס מופיע ברשימה; נשמר ב-`AppData.Animals` |

---

### TC-02

| שדה | ערך |
|-----|-----|
| **Test Case ID** | TC-02 |
| **Test Case Name** | הוספת חיה — שבב לא תקין |
| **Type** | Validation — Negative |
| **Test Data** | שבב: `1234567` (לא מתחיל ב-376) |
| **Expected Result** | הודעה: "אחרי 376 יש להזין בדיוק 4 ספרות"; החיה לא נשמרת |

---

### TC-03

| שדה | ערך |
|-----|-----|
| **Test Case ID** | TC-03 |
| **Test Case Name** | סינון קטalog — הצג כלבים בלבד |
| **Type** | Functional — Positive |
| **Test Data** | במערכת יש כלב וחתול; סינון: "כלב" |
| **Expected Result** | מוצגים רק כרטיסי כלב; חתולים מוסתרים |

---

### TC-04

| שדה | ערך |
|-----|-----|
| **Test Case ID** | TC-04 |
| **Test Case Name** | חיפוש חיה לפי שבב |
| **Type** | Functional — Positive |
| **Test Data** | חיפוש: `3761234` (שבב קיים) |
| **Expected Result** | מוצגת רק החיה עם שבב זה; טקסט "מציג 1 מתוך X" |

---

## 4. בדיקות Functional ו-GUI

### 4.1 שתי בדיקות Functional

**FT-01 — הוספת חיה ושמירה במסד**

| פריט | תיאור |
|------|--------|
| **מטרה** | לוודא שחיה חדשה נשמרת ב-SQLite |
| **Preconditions** | מחובר כמזכיר/ה או וטרינר/ית; קיים לפחות לקוח אחד |
| **Steps** | 1. פתח מסך בעלי חיים 2. לחץ "הוסף חיה חדשה" 3. מלא שדות תקינים 4. בחר בעלים 5. לחץ "שמור חיה" |
| **Expected** | הודעת הצלחה; החיה קיימת ב-`AppData.Animals` אחרי הפעלה מחדש |

**FT-02 — מניעת שבב כפול**

| פריט | תיאור |
|------|--------|
| **מטרה** | לוודא שלא ניתן לשמור שני שבבים זהים |
| **Preconditions** | חיה עם שבב `3765678` כבר קיימת |
| **Steps** | 1. הוסף חיה חדשה 2. הזן שבב `3765678` 3. לחץ שמור |
| **Expected** | הודעה: "מספר השבב כבר קיים במערכת..."; לא נשמר |

---

### 4.2 שתי בדיקות GUI

**GT-01 — פריסת מסך בעלי חיים**

| פריט | תיאור |
|------|--------|
| **מטרה** | לוודא שהמסך קריא ומאורגן |
| **Steps** | 1. התחבר כ-vet 2. פתח "בעלי חיים" |
| **Expected** | כותרת "ניהול בעלי חיים"; שדה חיפוש; ComboBox סינון; כרטיסי חיות; כפתור "חזרה לתפריט" |

**GT-02 — טופס הוספת חיה**

| פריט | תיאור |
|------|--------|
| **מטרה** | לוודא שטופס ההוספה נפתח ונסגר |
| **Steps** | 1. לחץ "הוסף חיה חדשה" 2. בדוק שדות 3. לחץ סגור |
| **Expected** | מופיעים שדות שם, סוג, שבב (376+4 ספרות), משקל, בעלים, תאריכים; הטופס נסגר בלי שגיאה |

---

## 5. תרחיש בדיקה + 2 מקרי בדיקה + תסריטי בדיקה

### תרחיש (Scenario)

**שם התרחיש:** הוספת חיה חדשה וסינון לפי סוג  
**User Story:** US-01 + US-02  
**תיאור:** מוסיפים חיה מסוג כלב, ואז מסננים את הרשימה כך שמוצגים רק כלבים.

---

### תסריט בדיקה 1 — TC-S01

| שדה | תוכן |
|-----|------|
| **Test Case ID** | TC-S01 |
| **Test Objective** | לוודא הוספת חיה חדשה מצליחה |
| **Preconditions** | מחובר כ-vet; לקוח "דני כהן" קיים במערכת; שבב `3769999` לא קיים |
| **Test Type** | Functional — End to End (חלק ראשון) |

**Steps:**

1. פתח מסך בעלי חיים
2. לחץ "הוסף חיה חדשה"
3. הזן שם: `רקס`
4. בחר סוג: כלב
5. הזן שבב: `3769999`
6. הזן משקל: `25`
7. בחר בעלים: דני כהן
8. לחץ "שמור חיה"

| **Expected Result** | הודעה "החיה נוספה בהצלחה"; כרטיס "רקס" מופיע ברשימה |
| **Actual Result** | הודעה "החיה נוספה בהצלחה"; כרטיס "רקס" מופיע ברשימה עם סוג כלב |
| **Pass/Fail** | **Pass** |

---

### תסריט בדיקה 2 — TC-S02

| שדה | תוכן |
|-----|------|
| **Test Case ID** | TC-S02 |
| **Test Objective** | לוודא שסינון "כלב" מציג רק כלבים |
| **Preconditions** | TC-S01 עבר; במערכת יש לפחות כלב וחתול |
| **Test Type** | Functional — End to End (חלק שני) |

**Steps:**

1. במסך בעלי חיים בחר סינון: "כלב"
2. בדוק את כרטיסי התוצאות

| **Expected Result** | מוצגים רק כרטיסי כלב; אין כרטיסי חתול |
| **Actual Result** | מוצגים 2 כרטיסי כלב בלבד; סינון "חתול" מציג 0 כרטיסים כלב |
| **Pass/Fail** | **Pass** |

---

## 6. Boundary Value Testing (בדיקות ערך גבול)

### טבלת גבולות — מספר שבב (7 ספרות, מתחיל 376)

| מזהה | ערך | תיאור | תוצאה צפויה |
|------|-----|--------|-------------|
| B-CH1 | `376123` (6) | קצר מדי | **לא תקין** |
| B-CH2 | `3761234` (7) | גבול תקין | **תקין** |
| B-CH3 | `37612345` (8) | ארוך מדי | **לא תקין** |
| B-CH4 | `1234567` | לא מתחיל ב-376 | **לא תקין** |
| B-CH5 | `37612A4` | תו לא-ספרתי | **לא תקין** |

---

### טבלת גבולות — משקל (0.1–100 ק"ג)

| מזהה | ערך | תוצאה צפויה |
|------|-----|-------------|
| B-W1 | `0` | **לא תקין** |
| B-W2 | `0.1` | **תקין** (גבול תחתון) |
| B-W3 | `50` | **תקין** |
| B-W4 | `100` | **תקין** (גבול עליון) |
| B-W5 | `100.1` | **לא תקין** |

---

### טבלת גבולות — שם חיה (אותיות בלבד)

| מזהה | ערך | תוצאה צפויה |
|------|-----|-------------|
| B-N1 | `` (ריק) | **לא תקין** |
| B-N2 | `באדי` | **תקין** |
| B-N3 | `Dog123` | **לא תקין** (ספרות) |
| B-N4 | `Max` | **תקין** |

---

### טבלת גבולות — סוג חיה (קטalog)

| מזהה | ערך | תוצאה צפויה |
|------|-----|-------------|
| B-S1 | `כלב` | **תקין** |
| B-S2 | `חתול` | **תקין** |
| B-S3 | `ארנב` | **לא תקין** |
| B-S4 | `Dog` | **תקין** (אנגלית נתמכת) |

---

## 7. Decision Table ו-Decision Tree — החלטת שמירת חיה

### תנאים (Conditions)

| # | תנאי | משמעות |
|---|------|--------|
| C1 | שם חיה תקין | `IsValidAnimalName(name)` |
| C2 | שבב תקין ולא כפול | `IsValidChipNumber` + לא קיים ב-DB |
| C3 | בעלים נבחר וקיים | `ownerId` לא null + לקוח קיים |

### פעולות (Actions)

| # | פעולה |
|---|--------|
| A1 | **שמור חיה** — הוספה ל-`AppData.Animals` |
| A2 | **הצג שגיאת ולידציה** — הודעה בעברית, לא שומר |
| A3 | **חסום — בעלים חסר** — "יש לבחור בעלים מהרשימה" |

---

### Decision Table — טבלת החלטות

| כלל | C1 שם | C2 שבב | C3 בעלים | פעולה |
|-----|-------|--------|----------|--------|
| R1 | לא | — | — | A2 |
| R2 | כן | לא | — | A2 |
| R3 | כן | כן | לא | A3 |
| R4 | כן | כן | כן | A1 |

*מימוש: `AnimalsView.AddAnimal_Click` — שורות 221–343*

---

### Decision Tree — עץ החלטות

```
              [לחיצה על "שמור חיה"]
                       |
            [האם שם תקין?]
               /        \
             לא          כן
              |           |
         [A2 שגיאה]  [האם שבב תקין?]
                        /        \
                      לא          כן
                       |           |
                  [A2 שגיאה]  [שבב כבר קיים?]
                               /           \
                             כן             לא
                              |              |
                         [A2 שגיאה]   [בעלים נבחר?]
                                        /         \
                                      לא           כן
                                       |            |
                                  [A3 חסום]    [A1 שמור]
```

---

## נספח — מיפוי לפרויקט

| נושא | קובץ |
|------|------|
| הוספת חיה | `Source/Frontend/Views/AnimalsView.axaml.cs` — `AddAnimal_Click` |
| ולידציה | `Source/Services/ValidationService.cs` |
| סינון / חיפוש | `AnimalsView.MatchesSearchAndFilter`, `RefreshAnimalsList` |
| שמירה | `Source/Data/AppData.cs` — `SaveAnimalsToDatabase` |

---

*מסמך זה מתאר ניהול בעלי חיים בלבד. לא בוצעו שינויים בקוד.*
