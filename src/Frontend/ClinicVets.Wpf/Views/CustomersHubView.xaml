<UserControl x:Class="ClinicVets.Wpf.Views.CustomersHubView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <TabControl x:Name="Tabs" BorderThickness="0" Background="Transparent">
    <TabItem Header="Search">
      <Grid Margin="0,12,0,0">
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto" />
          <RowDefinition Height="Auto" />
          <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Text="Search customers" FontSize="20" FontWeight="SemiBold" Foreground="{StaticResource Brush.Text}" />
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,16,0,0">
          <TextBox x:Name="Query" Width="360" />
          <Button Content="Search" Style="{StaticResource Btn.Primary}" Margin="12,0,0,0" Click="OnSearch" />
        </StackPanel>
        <DataGrid x:Name="Grid"
                  Grid.Row="2"
                  Margin="0,16,0,0"
                  AutoGenerateColumns="True"
                  IsReadOnly="True"
                  HeadersVisibility="Column"
                  CanUserAddRows="False"
                  BorderThickness="1"
                  BorderBrush="{StaticResource Brush.Border}" />
      </Grid>
    </TabItem>
    <TabItem Header="Register">
      <StackPanel Margin="0,12,0,0" MaxWidth="480">
        <TextBlock Text="Register customer" FontSize="20" FontWeight="SemiBold" Foreground="{StaticResource Brush.Text}" />
        <TextBlock Text="Full name" Style="{StaticResource Field.Caption}" />
        <TextBox x:Name="RegName" />
        <TextBlock Text="National ID (9 digits)" Style="{StaticResource Field.Caption}" />
        <TextBox x:Name="RegNationalId" MaxLength="9" />
        <TextBlock Text="Phone" Style="{StaticResource Field.Caption}" />
        <TextBox x:Name="RegPhone" />
        <TextBlock Text="Email" Style="{StaticResource Field.Caption}" />
        <TextBox x:Name="RegEmail" />
        <Button Content="Save customer" Style="{StaticResource Btn.Primary}" HorizontalAlignment="Left" Margin="0,20,0,0" Click="OnSaveCustomer" />
        <TextBlock x:Name="RegMessage" Margin="0,12,0,0" TextWrapping="Wrap" Visibility="Collapsed" />
      </StackPanel>
    </TabItem>
  </TabControl>
</UserControl>
