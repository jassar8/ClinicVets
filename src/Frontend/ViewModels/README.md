# ViewModels

This desktop app uses **Avalonia code-behind** (`.axaml` + `.axaml.cs`) per screen.
Business logic lives in `src/Backend/ClinicVets.Application` services; UI wiring is in `Services/` and view code-behind.

No separate ViewModel classes are required for the current scope.
